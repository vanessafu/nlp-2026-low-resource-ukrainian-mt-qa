# Low-Resource Ukrainian MT

Project scaffold for downloading and preparing raw Ukrainian MT parallel
corpora. Training-format conversion will be handled in a later script.

## Build Raw Parallel Data

From the repository root:

```bash
python 

You can change the output split or sampling options:

```bash


See `data/README.md` for accepted raw data formats and
`configs/data_mix.example.json` for a starting point to document corpus mixing.
