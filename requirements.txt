# ── Core data processing ──────────────────────────────────────────────────────
polars>=0.20.0
pandas>=2.0.0
pyarrow>=14.0.0

# ── HuggingFace ───────────────────────────────────────────────────────────────
datasets>=2.14.0
transformers>=4.35.0

# ── HTTP / download ───────────────────────────────────────────────────────────
requests>=2.31.0
# curl must be available as a system binary (used for MaCoCu download)

# ── Near-dedup (Stage 2) ──────────────────────────────────────────────────────
datasketch>=1.6.5   # preferred LSH; falls back to built-in MinHash if missing
mmh3>=4.0.0         # fast Murmur3 hashing; falls back to hashlib if missing

# ── Language identification (Stage 3) ─────────────────────────────────────────
fasttext-langdetect>=1.0.6   # lid.176.bin auto-downloaded on first run

# ── Semantic alignment (Stage 8) ──────────────────────────────────────────────
sentence-transformers>=2.3.0
# ONNX/int8 optimization (optional, ~3-5× CPU speedup on AVX512 nodes):
# optimum[onnxruntime]>=1.16.0

# ── QE scoring (Stage 9, optional) ────────────────────────────────────────────
# pymarian  — install from source: https://github.com/marian-nmt/pymarian

# ── Training (Qwen2.5-3B QLoRA) ───────────────────────────────────────────────
torch>=2.1.0
peft>=0.7.0
trl>=0.7.0
accelerate>=0.25.0
bitsandbytes>=0.41.0
scipy>=1.11.0
